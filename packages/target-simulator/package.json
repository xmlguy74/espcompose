{
  "name": "@espcompose/compose-target-simulator",
  "version": "0.0.1",
  "description": "ESPHome Compose Simulator — Browser-based LVGL UI preview for fast iteration",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean --external @espcompose/core --external esbuild",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "keywords": [
    "esphome",
    "simulator",
    "lvgl",
    "ui-preview"
  ],
  "license": "MIT",
  "dependencies": {
    "@espcompose/core": "workspace:*",
    "esbuild": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
